# url-shortener
Developed a URL Shortener service using Java and Spring Boot that converts long URLs into short, unique links. Implemented REST APIs for generating and retrieving shortened URLs and used hashing/UUID techniques to create unique codes. The system maps short URLs to original links and redirects users efficiently.
